---
# RKE2 HA server role — first host in the play bootstraps, the rest join.
# TODO(P2): full implementation —
#   - install rke2-server (pinned version) via https://get.rke2.io
#   - /etc/rancher/rke2/config.yaml: tls-san [xx.xx.100.10, hostnames],
#     cni: none (Cilium installed via HelmChartConfig), disable kube-proxy (Cilium KPR),
#     server: https://xx.xx.100.10:9345 for joining nodes, shared token
#   - CIS profile: profile: cis, etcd user, kernel params
#   - kube-vip static pod manifest (ARP mode, VIP xx.xx.100.10)
- name: Placeholder — implemented in P2
  ansible.builtin.debug:
    msg: "rke2-server role: implementation lands before P3 execution"
