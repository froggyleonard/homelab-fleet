apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: authentik-secrets-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - 02-secrets.sops.yaml
  # Operator creates this ciphertext before the activation diff can render.
  - 02b-postgres-admin.sops.yaml
