apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homarr-data
  namespace: homarr
  labels:
    app.kubernetes.io/name: homarr
    app.kubernetes.io/part-of: fleet
    app.kubernetes.io/managed-by: argocd
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homarr
  namespace: homarr
  labels:
    app.kubernetes.io/name: homarr
    app.kubernetes.io/part-of: fleet
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  strategy:
    type: Recreate # RWO volume
  selector:
    matchLabels:
      app.kubernetes.io/name: homarr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: homarr
        app.kubernetes.io/part-of: fleet
        app.kubernetes.io/managed-by: argocd
    spec:
      containers:
        - name: homarr
          image: ghcr.io/homarr-labs/homarr:v1.72.0
          ports:
            - name: http
              containerPort: 7575
          env:
            - name: SECRET_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: homarr-secrets
                  key: encryption-key
            # credentials fallback stays until OIDC is proven; drop it to go SSO-only
            - name: AUTH_PROVIDERS
              value: oidc,credentials
            - name: AUTH_OIDC_ISSUER
              value: https://auth.froggyleonard.com/application/o/homarr/
            - name: AUTH_OIDC_CLIENT_ID
              value: HcLyCrofMGNstdxy3qqQvoCOAKwB2heMs2xV2AHc
            - name: AUTH_OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: homarr-secrets
                  key: oidc-client-secret
            - name: AUTH_OIDC_CLIENT_NAME
              value: Authentik
            - name: AUTH_LOGOUT_REDIRECT_URL
              value: https://auth.froggyleonard.com/application/o/homarr/end-session/
          volumeMounts:
            - name: data
              mountPath: /appdata
          readinessProbe:
            httpGet:
              path: /api/health/ready
              port: 7575
            initialDelaySeconds: 15
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /api/health/live
              port: 7575
            initialDelaySeconds: 45
            periodSeconds: 20
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 2000m
              memory: 768Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: homarr-data
---
apiVersion: v1
kind: Service
metadata:
  name: homarr
  namespace: homarr
  labels:
    app.kubernetes.io/name: homarr
    app.kubernetes.io/part-of: fleet
    app.kubernetes.io/managed-by: argocd
spec:
  selector:
    app.kubernetes.io/name: homarr
  ports:
    - name: http
      port: 80
      targetPort: 7575
