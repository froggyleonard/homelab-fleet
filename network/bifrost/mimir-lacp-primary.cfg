! GATED: apply only after the host bond is live and reachable over Gi0/4/Po2.
! The Talos trial must still have sufficient time remaining for verification.
configure terminal
interface GigabitEthernet0/2
 description Mimir LACP member
 switchport mode access
 switchport access vlan 100
 channel-group 2 mode active
end
