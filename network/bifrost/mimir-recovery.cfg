! Approved recovery stage only; not the final LACP configuration.
! Confirm this physical interface is the intended Mimir link before applying.
configure terminal
interface GigabitEthernet0/2
 switchport mode access
 switchport access vlan 100
end
