! Conditional second-link recovery after the first link is verified forwarding.
! Both cables must be independently confirmed as belonging to Mimir.
! Do not apply if LACP or a host bridge is already configured unexpectedly.
configure terminal
interface GigabitEthernet0/4
 switchport mode access
 switchport access vlan 100
end
