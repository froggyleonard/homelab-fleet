! GATED: confirm live NIC-to-port mapping and reviewed Talos try-mode rollback.
! Stage only the secondary link. Keep Gi0/2 independent for host recovery.
! If the node becomes unreachable, undo only Gi0/4 channel-group membership.
configure terminal
interface Port-channel2
 description Mimir LACP access VLAN 100
 switchport mode access
 switchport access vlan 100
 spanning-tree portfast
interface GigabitEthernet0/4
 description Mimir LACP member
 switchport mode access
 switchport access vlan 100
 channel-group 2 mode active
end
